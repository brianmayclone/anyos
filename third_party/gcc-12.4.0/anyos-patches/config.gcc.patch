Patches applied programmatically by scripts/build_gcc_toolchain.sh
==================================================================

The build script applies the following patches via sed to the GCC and
Binutils source trees.  This file documents the changes for reference.

1. config.sub (binutils + GCC)
   Add "anyos*" to the recognized OS list.

2. bfd/config.bfd (binutils)
   Map x86_64-*-anyos* to x86_64_elf64_vec (standard ELF64).

3. gas/configure.tgt (binutils)
   Map x86_64-*-anyos* to fmt=elf.

4. ld/configure.tgt (binutils)
   Map x86_64-*-anyos* to targ_emul=elf_x86_64.

5. gcc/config.gcc
   Add common OS stanza (*-*-anyos*) with gas=yes, gnu_ld=yes, cxa_atexit.
   Add machine stanza (x86_64-*-anyos*) with tm_file including anyos.h.

6. gcc/config/anyos.h (new file)
   OS-specific target configuration — see gcc-config-anyos.h.

7. libgcc/config.host
   Map x86_64-*-anyos* to produce crtbegin.o, crtend.o.

8. fixincludes/mkfixinc.sh
   Disable fixincludes for anyos target.
