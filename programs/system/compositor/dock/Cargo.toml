[package]
name = "dock"
version = "0.1.0"
edition = "2021"

[dependencies]
anyos_std = { path = "../../../../stdlib" }
libcompositor_client = { path = "../../../dll/libcompositor_client" }

[profile.dev]
panic = "abort"
opt-level = 2
