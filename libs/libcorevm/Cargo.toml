[package]
name = "libcorevm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib", "rlib"]

[features]
default = ["anyos"]
anyos = []
host_test = []
linux = ["std"]
windows = ["std"]
std = []

[dependencies]
libheap = { path = "../libheap" }
libsyscall = { path = "../libsyscall" }

[workspace]
