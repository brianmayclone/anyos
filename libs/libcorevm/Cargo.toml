[package]
name = "libcorevm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib", "rlib"]

[features]
default = []
host_test = []

[dependencies]
libheap = { path = "../libheap" }
libsyscall = { path = "../libsyscall" }

[workspace]
