[package]
name = "libstd"
version = "0.1.0"
edition = "2021"

[dependencies]
anyos_std = { path = "../stdlib" }

[profile.dev]
panic = "abort"
opt-level = 2

[workspace]

[profile.release]
panic = "abort"
