[package]
name = "libwebview"
version = "0.1.0"
edition = "2021"

[dependencies]
anyos_std = { path = "../stdlib" }
libanyui_client = { path = "../libanyui_client" }
libfont_client = { path = "../libfont_client" }
libjs = { path = "../libjs" }

[features]
debug_surf = []
host = ["anyos_std/host", "libanyui_client/host", "libfont_client/host", "libjs/host"]

[lib]
name = "libwebview"
