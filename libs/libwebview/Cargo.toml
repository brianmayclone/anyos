[package]
name = "libwebview"
version = "0.1.0"
edition = "2021"

[dependencies]
anyos_std = { path = "../stdlib" }
libanyui_client = { path = "../libanyui_client" }
libjs = { path = "../libjs" }

[lib]
name = "libwebview"
