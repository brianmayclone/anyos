[package]
name = "libdb_client"
version = "0.1.0"
edition = "2021"

[features]
default = []
host = ["dynlink/host", "anyos_std/host"]

[dependencies]
dynlink = { path = "../dynlink" }
anyos_std = { path = "../stdlib" }

[lib]
name = "libdb_client"
