[package]
name = "libcorevm-hosttests"
version = "0.1.0"
edition = "2021"

[dependencies]
libcorevm = { path = "../libcorevm", features = ["host_test"] }

[profile.release]
lto = "fat"
codegen-units = 1

[workspace]
