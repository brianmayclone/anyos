//! libgl_client — Safe Rust wrapper for the libgl shared library.
//!
//! Loads `libgl.so` via `dl_open`/`dl_sym` and provides ergonomic Rust types
//! for OpenGL ES 2.0 operations.

#![no_std]

extern crate alloc;

// Placeholder — full implementation follows
