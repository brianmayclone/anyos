[package]
name = "alloc"
version = "0.1.0"
edition = "2021"

[lib]
name = "alloc"
path = "src/lib.rs"

[dependencies]
core = { path = "../core" }

[profile.dev]
panic = "abort"
opt-level = 2

[profile.release]
panic = "abort"
