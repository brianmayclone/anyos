[package]
name = "libzip"
version = "0.1.0"
edition = "2021"

[features]
host = []

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
libheap = { path = "../libheap" }
libsyscall = { path = "../libsyscall" }

[profile.dev]
panic = "abort"
opt-level = 2

[profile.release]
panic = "abort"
