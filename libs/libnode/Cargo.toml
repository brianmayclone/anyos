[package]
name = "libnode"
version = "0.1.0"
edition = "2021"

[features]
host = ["anyos_std/host", "libhttp_client/host", "libjs/host", "libuv/host"]

[dependencies]
anyos_std = { path = "../stdlib" }
libhttp_client = { path = "../libhttp_client" }
libjs = { path = "../libjs" }
libuv = { path = "../libuv" }

[lib]
name = "libnode"
