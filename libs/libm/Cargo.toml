[package]
name = "libm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

# No dependencies — pure computation, no heap needed

[profile.dev]
panic = "abort"
opt-level = 2

[profile.release]
panic = "abort"

[workspace]
