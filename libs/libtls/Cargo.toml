[package]
name = "libtls"
version = "0.1.0"
edition = "2021"

[features]
default = []
host = []

[dependencies]

[profile.dev]
panic = "abort"
opt-level = 2

[profile.release]
panic = "abort"

[workspace]
