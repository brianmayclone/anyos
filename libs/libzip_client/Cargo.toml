[package]
name = "libzip_client"
version = "0.1.0"
edition = "2021"

[features]
host = ["dynlink/host", "anyos_std/host", "dep:libzip", "libzip/host"]

[dependencies]
dynlink = { path = "../dynlink" }
anyos_std = { path = "../stdlib" }
libzip = { path = "../libzip", optional = true }

[lib]
name = "libzip_client"
