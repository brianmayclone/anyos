[package]
name = "libfont_client"
version = "0.1.0"
edition = "2021"

[features]
host = ["dynlink/host", "anyos_std/host"]

[dependencies]
dynlink = { path = "../dynlink" }
anyos_std = { path = "../stdlib" }

[profile.dev]
opt-level = 2
