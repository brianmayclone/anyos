#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

cargo check --manifest-path libs/libcorevm/Cargo.toml
cargo +stable test --manifest-path libs/libcorevm-hosttests/Cargo.toml --target x86_64-unknown-linux-gnu
