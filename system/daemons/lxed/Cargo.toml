[package]
name = "lxed"
version = "0.1.0"
edition = "2021"

[dependencies]
anyos_std = { path = "../../../libs/stdlib" }
liblxecore = { path = "../../../libs/liblxecore" }
libsvc = { path = "../../../libs/libsvc" }

[profile.dev]
panic = "abort"
opt-level = 2

[profile.release]
panic = "abort"
