[package]
name = "confd"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
anyos_std = { path = "../../../libs/stdlib" }
libdb_client = { path = "../../../libs/libdb_client" }
libsvc = { path = "../../../libs/libsvc" }

[profile.dev]
panic = "abort"
opt-level = 2

[profile.release]
panic = "abort"
