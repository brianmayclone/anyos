[package]
name = "compositor"
version = "0.1.0"
edition = "2021"

[dependencies]
anyos_std = { path = "../../../libs/stdlib" }
libami = { path = "../../../libs/libami" }
libconf = { path = "../../../libs/libconf" }
librender_client = { path = "../../../libs/librender_client" }
libimage_client = { path = "../../../libs/libimage_client" }
libfont_client = { path = "../../../libs/libfont_client" }
libconf_schema = { path = "../../../libs/libconf_schema" }

[profile.dev]
panic = "abort"
opt-level = 2
