[package]
name = "permdialog"
version = "0.1.0"
edition = "2021"

[dependencies]
anyos_std = { path = "../../../libs/stdlib" }
libanyui_client = { path = "../../../libs/libanyui_client" }
dynlink = { path = "../../../libs/dynlink" }

[profile.dev]
panic = "abort"
opt-level = 2

[profile.release]
panic = "abort"
