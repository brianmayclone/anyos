[package]
name = "shell"
version = "0.1.0"
edition = "2021"

[dependencies]
anyos_std = { path = "../../../libs/stdlib" }
libshellcommon = { path = "../../../libs/libshellcommon" }

[profile.dev]
panic = "abort"
opt-level = 2

[profile.release]
panic = "abort"
