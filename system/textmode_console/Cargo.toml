[package]
name = "textmode_console"
version = "0.1.0"
edition = "2021"

[dependencies]
anyos_std = { path = "../../libs/stdlib" }
libconf_schema = { path = "../../libs/libconf_schema" }
libshellcommon = { path = "../../libs/libshellcommon" }

[profile.dev]
panic = "abort"
opt-level = 2

[profile.release]
panic = "abort"
