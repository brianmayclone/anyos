[package]
name = "vmd"
version = "0.1.0"
edition = "2021"

[dependencies]
anyos_std = { path = "../../libs/stdlib" }
dynlink = { path = "../../libs/dynlink" }
libcorevm_client = { path = "../../libs/libcorevm_client" }

[profile.dev]
panic = "abort"
opt-level = 2

[profile.release]
panic = "abort"
