[package]
name = "wget"
version = "0.1.0"
edition = "2021"

[dependencies]
anyos_std = { path = "../../libs/stdlib" }
libtls = { path = "../../libs/libtls" }

[profile.dev]
panic = "abort"
opt-level = 2

[profile.release]
panic = "abort"
