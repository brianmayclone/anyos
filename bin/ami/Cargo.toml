[package]
name = "ami"
version = "0.1.0"
edition = "2021"

[dependencies]
anyos_std = { path = "../../libs/stdlib" }
libami = { path = "../../libs/libami" }

[profile.dev]
panic = "abort"
opt-level = 2

[profile.release]
panic = "abort"
