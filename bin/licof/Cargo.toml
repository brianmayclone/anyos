[package]
name = "licof"
version = "0.1.0"
edition = "2021"

[dependencies]
anyos_std = { path = "../../libs/stdlib" }
libhttp_client = { path = "../../libs/libhttp_client" }
libzip_client = { path = "../../libs/libzip_client" }

[profile.dev]
panic = "abort"
opt-level = 2

[profile.release]
panic = "abort"
