[package]
name = "jp2a"
version = "0.1.0"
edition = "2021"

[dependencies]
anyos_std = { path = "../../libs/stdlib" }
libimage_client = { path = "../../libs/libimage_client" }

[profile.dev]
panic = "abort"
opt-level = 2

[profile.release]
panic = "abort"
